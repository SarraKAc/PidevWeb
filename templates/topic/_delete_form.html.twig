<form method="post" action="{{ path('app_topic_delete', {'id_topic': topic.id_topic}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ topic.id_topic) }}">
    <button class="btn">Supprimer</button>
</form>
