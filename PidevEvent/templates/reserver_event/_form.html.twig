{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
    <button class="btn" style="background-color: blue; color: white;">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}